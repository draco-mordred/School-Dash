# V.A.L. — Security & Middleware Subsystem

This internal module owns authentication, authorization, and global middleware logic.

Primary responsibilities:
- JWT handling and auth validation
- role-based access control
- CORS, request parsing, and timeout middleware
